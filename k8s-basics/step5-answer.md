`kubectl create -f liveness.yml`{{execute}}.
`kubectl apply -f liveness.yml`{{execute}}.
`kubectl get pods -l app=liveness`{{execute}}.
`kubectl apply -f liveness.yml`{{execute}}.
`kubectl get pods -l app=liveness`{{execute}}.